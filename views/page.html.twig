{% extends 'layout.html.twig' %}

{% block bodyContent %}
    <div class="container">
        {% if headerImage is defined %}
        <div class="row">
            <div class="col-md-12 header" style="background-image: url(/images/header/{{ headerImage }});">

            </div>
        </div>
        {% endif %}

        <div class="row m-t-1">
            <div class="col-md-12">
                <ol class="breadcrumb">
                    <li>
                        <a href="{{ path('page', { slug: 'index' }) }}">
                            Home
                        </a>
                    </li>
                    <li class="active">
                        Studium
                    </li>
                </ol>
            </div>
        </div>

        {% block pageContent %}
        {% endblock %}

        <div class="row m-b-1">
            <div class="col-md-12">
                <footer class="text-xs-center">
                    Malte Hübner &bull; <a href="{{ path('page', { slug: 'impressum' }) }}">Impressum</a> &bull; <a href="http://sqi.be/h140g">GitHub</a>
                </footer>
            </div>
        </div>
    </div>
{% endblock %}
