{% extends 'layout.html.twig' %}

{% block bodyContent %}
    <div class="container">
        {% block breadcrumbContainer %}
        {% if block('breadcrumb') %}
        <div class="row mt-3 mb-3">
            <div class="col-md-12">
                <nav aria-label="breadcrumb" role="navigation">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">
                            <a href="/">
                                Home
                            </a>
                        </li>
                        {% block breadcrumb %}{% endblock %}
                    </ol>
                </nav>
            </div>
        </div>
        {% endif %}
        {% endblock %}

        {% block pageContent %}
        {% endblock %}

        <div class="row mt-3 mb-2">
            <div class="col-md-12">
                <footer class="text-center">
                    Malte Hübner
                    &bull;
                    <a href="{{ path('page', { slug: 'privacy' }) }}">Datenschutzerklärung</a>
                    &bull;
                    <a href="{{ path('page', { slug: 'impressum' }) }}">Impressum</a>
                    &bull;
                    <a href="http://sqi.be/h140g">GitHub</a>
                </footer>
            </div>
        </div>
    </div>
{% endblock %}
